// noinspection ES6PreferShortImport

// TODO Impl. renderThree unit tests (three/examples/jsm/ import issue)
//import {renderThree} from '../src/renderThree';

describe('renderThree', () => {
    afterEach(() => {
        jest.restoreAllMocks();
    });

    it('dummy test', () => {
        expect(true).toBeTruthy();
    });
});
