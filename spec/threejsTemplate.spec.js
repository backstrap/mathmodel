// noinspection ES6PreferShortImport

// TODO Impl. threejsTemplate unit tests
import {threejsTemplate} from '../src/threejsTemplate';

describe('threejsTemplate', () => {
    afterEach(() => {
        jest.restoreAllMocks();
    });

    it('dummy test', () => {
        expect(true).toBeTruthy();
    });
});
